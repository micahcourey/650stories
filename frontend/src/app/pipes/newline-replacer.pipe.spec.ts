import { NewlineReplacerPipe } from './newline-replacer.pipe';

describe('NewlineReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
